/**const express =require("express");
const routes = express.Router();
const contactform = require("../controllers/contact- controller");

routes.route("/contact").post(contactform);

module.exports = routes;**/